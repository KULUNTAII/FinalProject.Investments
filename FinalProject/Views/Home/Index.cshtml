@{
    ViewData["Title"] = "Home Page";
}

<!DOCTYPE html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewData["Title"]</title>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <link href="https://fonts.googleapis.com/css2?family=Poiret+One&display=swap" rel="stylesheet">

</head>

<body class="home_body">
    <div class="home_container">
        <h2 class="home_text4">Welcome to</h2>
        <h1 class="welcome">INVEST KG</h1>
        <h2 class="home_text1">Stop chasing your dream.</h2>
        <h2 class="home_text3">Face it!</h2>
        <h3 class="home_text2">Create. Invest. Engage.</h3>
        <div class="home_buttons">
            <a href="@Url.Action("Register", "Home")" class="home_btn">Register</a>
            <a href="@Url.Action("Login", "Home")" class="home_btn">Login</a>
        </div>
    </div>

    @* <script>
        $(document).ready(function () {
            var body = $("body");
            body.fadeIn(600);

            $(document).on("click", "a:not([href^='#']):not([href^='tel']):not([href^='mailto'])", function (e) {
                e.preventDefault();
                $("body").fadeOut(600);
                var self = this;
                setTimeout(function () {
                    window.location.href = $(self).attr("href");
                }, 1000);
            });
        });
    </script> *@
</body>
</html>