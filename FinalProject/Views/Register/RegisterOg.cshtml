@*  @using FinalProject.Application.Contracts
@model UserCreateDto


        <h1>Registration</h1>


        <form asp-controller="Register" asp-action="RegisterForm">
            <div>
                <label>Login</label>
                <input class="form-control ml-2" type="text"
                       placeholder="spiderman3000"
                       asp-for="Login"
                       required />
                <label>Password</label>
                <input class="form-control ml-2" type="text"
                       placeholder="Use secure and complex passwords."
                       asp-for="Password"
                       required />

                <label>Type</label>
        <select class="form-control" asp-for="Role" required>
            <option value="">Select role</option>
            @foreach (var role in (List<string>)ViewBag.Roles)
            {
                <option value="@role">@role</option>
            }
        </select>
        <span asp-validation-for="Role" class="text-danger"></span>
    </div>
    <button type="submit" class="btn btn-primary">Next</button>
        </form>

 *@ 